block('carousel').elem('img')(
    tag()('img'),
    attrs()(function() {
        var ctx = this.ctx;
        return {
            src: ctx.url,
            alt: ctx.alt,
            title: ctx.title
        };
    })
);
